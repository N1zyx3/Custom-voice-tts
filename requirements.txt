# Force CUDA torch build (plain `pip install` pulls CPU-only torch on Windows).
--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.14.0+cu130
torchaudio==2.11.0+cu130
qwen-tts
soundfile